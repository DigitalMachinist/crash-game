{
  "name": "crash-game",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "npm run dev:client & npm run dev:server",
    "dev:server": "npx wrangler dev",
    "dev:client": "npx vite build --watch --mode development",
    "build:client": "npx vite build",
    "test": "npx vitest run",
    "test:svelte": "npx vitest run --config vitest.svelte.config.ts",
    "test:workers": "npx vitest run --config vitest.workers.config.ts",
    "test:all": "npm run test && npm run test:svelte && npm run test:workers",
    "test:coverage": "npx vitest run --coverage",
    "typecheck": "npx tsc --noEmit",
    "typecheck:server": "npx tsc --project tsconfig.server.json --noEmit",
    "lint": "npx biome lint src",
    "format": "npx biome format src --write",
    "check": "npx biome check src",
    "prepare": "simple-git-hooks"
  },
  "dependencies": {
    "partyserver": "^0.3.3",
    "partysocket": "^1.1.16"
  },
  "engines": {
    "node": ">=24.14.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.6",
    "@cloudflare/vitest-pool-workers": "^0.13.2",
    "@cloudflare/workers-types": "^4.20260310.1",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/svelte": "^5.3.1",
    "@vitest/coverage-v8": "^4.1.0",
    "fast-check": "^4.6.0",
    "jsdom": "^24.1.3",
    "lint-staged": "^16.3.3",
    "simple-git-hooks": "^2.13.1",
    "svelte": "^5.53.10",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.0",
    "wrangler": "^4.75.0"
  },
  "simple-git-hooks": {
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "src/**/*.{ts,svelte}": [
      "npx biome check --write --no-errors-on-unmatched",
      "git add"
    ]
  }
}
